export const ASSET_TYPES = Object.freeze(['vehicule', 'equipement', 'dispositif_reutilisable']);

// Les équipements vivants sont créés uniquement à partir d'une saisie terrain.
// Aucun véhicule, numéro de série ou code de parc n'est déduit de l'inventaire documentaire.
export const OPERATIONAL_ASSETS = Object.freeze([]);

