{
  "name": "releve-smur-urgences",
  "version": "0.5.0-p0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check src/main.js"
  },
  "engines": {
    "node": ">=20"
  }
}
